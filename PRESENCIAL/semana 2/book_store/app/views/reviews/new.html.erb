<% if flash[:alert] %>
	<%= flash[:alert] %>
<% end %>

<% if @review.errors.any? %>
	<ul>
		<% @review.errors.full_messages.each do |error_msg| %>
			<li><%= error_msg %></li>
		<% end %>
	</ul>
<% end %>

<h2> AÑADIR REVIEW AL LIBRO <%= @book.name %> </h2>

<%= render "form" %>

<br>
<%= link_to("Cancelar", book_path(@book)) %>