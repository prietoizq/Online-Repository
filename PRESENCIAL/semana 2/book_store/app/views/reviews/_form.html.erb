<%= form_for [@book, @review] do |f| %>

	INTRODUCE TU NOMBRE DE USUARIO
	<%= f.text_field :user %>
	<br>

	<%= f.label :rating %>
	<%= f.number_field :rating %>
	<br>

	<%= f.label :comment %>
	<%= f.text_area :comment %>
	<br>

	<%= f.submit 'Save' %>

<% end %>