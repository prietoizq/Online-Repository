<h1> LAST 10 PROJECTS </h1>

<ul>
	<% @projects.each do |project| %>
		<li><%= project.name %>
			<% if project.description != nil %>
				<%= " - #{project.description}" %>
			<% end %>
		</li>
	<% end %>
</ul>